#include <iostream>     // Подключение библиотек
using namespace std;
 
const unsigned short size=5;  // Размер динамического массива
 
int main()
{
   char list[5][5], str[size];      // Массив строк
   int* array = new int[size];  // Выделили в куче память для 6 переменных типа int
   int n, m, len=size;
  
  for(int i = 0; i < size; i++)    
    {
        cin>>str;    
        for(int j = 0; j < 5; j++)   
        {
           list[i][j]=str[j];
        };
        str[0]=0;
    }
  
   for (int i = 0; i < size; i++)  // Проходимся по строке и переводим char в int для 
   {				 // динамического массива
    n=0;
    while (list[i][n]!='\0')
    {
        array[i]=10*array[i]+(list[i][n]-'0');
        n+=1;
    }
   };
   
  while(len--)                         // Проводим сортировку “пузырьком” по возрастанию
   {                                         
	 bool swapped = false;                     
	 for(int i = 0; i < len; i++)  
	   {
          if(array[i] > array[i + 1])
		    {
			 swap(array[i], array[i + 1]);
			 swapped = true;
			}
	    }
	 if(swapped == false)
	 break;
	 } 
   
   for (int i = 0; i < size; ++i)     // Выводим отсортированный массив
   {
    cout<< *(array + i) << " ";
   };
   
   delete[] array;     // Освобождаем выделенную под массив память
   return 0;
}
